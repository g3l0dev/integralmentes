---
interface Props {
  url: string;
  body: string;
  class?:string
}
const { url, body } = Astro.props;
---

<a class=`${Astro.props.class} bg-[#468681] text-[#fafbfc] py-3 px-5 rounded-full` href={url}
  >{body}</a
>
